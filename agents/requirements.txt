# SOC AI Agent Framework - Python Dependencies
# متطلبات بايثون لإطار عمل وكلاء مركز العمليات الأمنية

# OpenSearch client
opensearch-py>=2.4.0,<3.0.0

# HTTP client for API calls (Wazuh, MISP, Ollama)
httpx>=0.27.0,<1.0.0

# Redis for inter-agent message bus
redis>=5.0.0,<6.0.0

# YAML configuration
PyYAML>=6.0.1,<7.0.0

# Prometheus metrics
prometheus-client>=0.20.0,<1.0.0

# Email (SMTP) - stdlib, no extra dep needed
# Scheduling
schedule>=1.2.0,<2.0.0

# Type validation
pydantic>=2.5.0,<3.0.0

# Async support
anyio>=4.0.0,<5.0.0
