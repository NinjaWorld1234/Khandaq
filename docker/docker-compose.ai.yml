###############################################################################
# SOC Platform — AI Agents Infrastructure
# مركز العمليات الأمنية — بنية وكلاء الذكاء الاصطناعي
#
# Services: Ollama (Local LLMs), Redis (Message Queue), AI Agents
#
# Usage:
#   docker compose -f docker-compose.ai.yml --env-file ../.env up -d
#
# Prerequisites:
#   - NVIDIA Container Toolkit for GPU passthrough
#   - agents/Dockerfile must exist for the ai-agents service
###############################################################################

version: "3.8"

services:
  # ===========================================================================
  # vLLM Commander — Serving Qwen-2.5-7B
  # محرك القيادة — تشغيل نموذج كوين للقائد
  # ===========================================================================
  vllm-commander:
    image: vllm/vllm-openai:latest
    container_name: soc-vllm-commander
    hostname: vllm-commander
    restart: unless-stopped
    shm_size: '8gb'
    command: --model /root/models/Commander-Qwen --served-model-name "Qwen/Qwen2.5-7B-Instruct-GGUF" --port 8000 --max-model-len 32768 --gpu-memory-utilization 0.9 --trust-remote-code
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ../soc_models:/root/models:ro
    ports:
      - "8000:8000"
    networks:
      - soc-net
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/v1/models || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ===========================================================================
  # vLLM Workers — Serving WhiteRabbitNeo-13B (Cybersecurity-Specialized LLM)
  # محرك العمال — تشغيل نموذج WhiteRabbitNeo المتخصص بالأمن السيبراني
  # ===========================================================================
  vllm-workers:
    image: vllm/vllm-openai:latest
    container_name: soc-vllm-workers
    hostname: vllm-workers
    restart: unless-stopped
    shm_size: '8gb'
    command: --model /root/models/WhiteRabbitNeo-13B --served-model-name "WhiteRabbitNeo/WhiteRabbitNeo-13B-v1" --port 8001 --max-model-len 8192 --gpu-memory-utilization 0.9
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ../soc_models:/root/models:ro
    ports:
      - "8001:8001"
    networks:
      - soc-net
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8001/v1/models || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ===========================================================================
  # Redis Primary — Message queue for AI agent communication
  # ريديس الأساسي — قائمة رسائل لتواصل وكلاء الذكاء الاصطناعي
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: soc-redis-ai-primary
    hostname: redis-ai
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}
      --maxmemory 1gb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec
    volumes:
      - redis-ai-data:/data
    ports:
      - "${REDIS_AI_PORT:-6379}:6379"
    networks:
      - soc-net
    deploy:
      resources:
        limits:
          memory: 1g
    healthcheck:
      test:
        [
          "CMD",
          "redis-cli",
          "-a",
          "${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}",
          "ping",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ===========================================================================
  # Redis Replica — High Availability for Message queue
  # ريديس الاحتياطي — لضمان استمرارية التواصل بين الوكلاء إذا سقط الأساسي
  # ===========================================================================
  redis-replica:
    image: redis:7-alpine
    container_name: soc-redis-ai-replica
    hostname: redis-ai-replica
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}
      --masterauth ${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}
      --replicaof redis-ai 6379
      --maxmemory 1gb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis-ai-replica-data:/data
    networks:
      - soc-net
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1g

  # ===========================================================================
  # Qdrant — Vector Database for Cyber Memory Engine
  # قاعدة بيانات المتجهات — للذاكرة السيبرانية الحية
  # ===========================================================================
  qdrant:
    image: qdrant/qdrant:latest
    container_name: soc-qdrant
    hostname: qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - qdrant-data:/qdrant/storage
    networks:
      - soc-net
    deploy:
      resources:
        limits:
          memory: 2g

  # ===========================================================================
  # Neo4j — Graph Database for Live Attack Graph
  # قاعدة بيانات الرسم البياني — لتمثيل هجمات الشبكة
  # ===========================================================================
  neo4j:
    image: neo4j:5.14
    container_name: soc-neo4j
    hostname: neo4j
    restart: unless-stopped
    environment:
      - NEO4J_AUTH=neo4j/${NEO4J_PASSWORD:-Ch@ngeMe_Neo4j_2024!}
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j-data:/data
    networks:
      - soc-net
    deploy:
      resources:
        limits:
          memory: 2g

  # ===========================================================================
  # Deception Mesh — Honeypot Node
  # شبكة الخداع — المصائد الوهمية لاكتشاف المهاجمين فورياً
  # ===========================================================================
  deception-node:
    build:
      context: ../
      dockerfile: docker/Dockerfile.deception
    container_name: soc-deception-node
    hostname: deception-node
    restart: unless-stopped
    ports:
      - "2222:2222" # Fake SSH
      - "3389:3389" # Fake RDP
      - "5900:5900" # Fake VNC
    environment:
      - REDIS_HOST=redis-ai
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}
    networks:
      - soc-net
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512m
  # ===========================================================================
  # AI Agents — Custom SOC AI agents (52 agents)
  # وكلاء الذكاء الاصطناعي — وكلاء ذكاء اصطناعي مخصصون (52 وكيل)
  # ===========================================================================
  ai-agents:
    build:
      context: ../agents
      dockerfile: Dockerfile
    image: soc-ai-agents:latest
    container_name: soc-ai-agents
    hostname: ai-agents
    restart: unless-stopped
    environment:
      # vLLM connection / اتصال محركات الذكاء المتعددة
      - VLLM_COMMANDER_URL=http://vllm-commander:8000/v1
      - VLLM_COMMANDER_MODEL=Qwen/Qwen2.5-7B-Instruct-GGUF
      - VLLM_WORKER_URL=http://vllm-workers:8001/v1
      - VLLM_WORKER_MODEL=QuantFactory/Meta-Llama-3-8B-Instruct-GGUF
      # Redis connection / اتصال ريديس
      - REDIS_HOST=redis-ai
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_AI_PASSWORD:-Ch@ngeMe_Redis_AI_2024!}
      # OpenSearch connection / اتصال محرك البحث
      - OPENSEARCH_HOSTS=["https://opensearch-node1:9200"]
      - OPENSEARCH_PASSWORD=${OPENSEARCH_INITIAL_ADMIN_PASSWORD:-admin}
      # Wazuh API connection / اتصال واجهة وازه
      - WAZUH_API_URL=https://wazuh-manager:55000
      - WAZUH_API_USER=${WAZUH_API_USER:-wazuh-wui}
      - WAZUH_API_PASSWORD=${WAZUH_API_PASSWORD:-Ch@ngeMe_Wazuh_API_2024!}
      # Threat Intel & SOAR / الاستخبارات والأتمتة
      - MISP_API_KEY=${MISP_API_KEY:-}
      - IRIS_API_KEY=${IRIS_API_KEY:-}
      # Alerting / التنبيهات الخارجية
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-}
      # Agent configuration / إعدادات الوكلاء
      - LOG_LEVEL=${AI_AGENTS_LOG_LEVEL:-INFO}
      - WORKERS=${AI_AGENTS_WORKERS:-4}
      - ENABLE_SELF_EVALUATION=true
      - ENABLE_DECEPTION=true
      - TZ=${TZ:-Asia/Riyadh}
      - HUGGINGFACE_HUB_CACHE=/app/.cache/huggingface
    volumes:
      - ai-agents-data:/app/data
      - ai-agents-logs:/app/logs
      - ai-agents-hf-cache:/app/.cache/huggingface
      - soc-intel-data:/app/data/intel_feeds:ro
      - ../soc_models:/app/models
    networks:
      - soc-net
    depends_on:
      vllm-commander:
        condition: service_healthy
      vllm-workers:
        condition: service_healthy
      redis:
        condition: service_healthy
      redis-replica:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 4g
        reservations:
          memory: 1g
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python -c 'import urllib.request; urllib.request.urlopen(\"http://vllm-commander:8000/health\")' || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

# =============================================================================
# Volumes — Persistent storage
# وحدات التخزين — التخزين الدائم
# =============================================================================
volumes:
  redis-ai-data:
    driver: local
    name: soc-redis-ai-data
  redis-ai-replica-data:
    driver: local
    name: soc-redis-ai-replica-data
  qdrant-data:
    driver: local
    name: soc-qdrant-data
  neo4j-data:
    driver: local
    name: soc-neo4j-data
  ai-agents-data:
    driver: local
    name: soc-ai-agents-data
  ai-agents-logs:
    driver: local
    name: soc-ai-agents-logs
  ai-agents-hf-cache:
    driver: local
    name: soc-ai-agents-hf-cache
  soc-intel-data:
    external: true
    name: soc-intel-data

# =============================================================================
# Networks — Shared SOC network
# الشبكات — شبكة مركز العمليات المشتركة
# =============================================================================
networks:
  soc-net:
    external: true
    name: soc-net
